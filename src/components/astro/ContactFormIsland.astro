---
// Server Island wrapper for ContactForm
// This ensures Actions endpoints are available even when the page is prerendered
// The server:defer directive makes this component render on-demand, which enables Actions
import ContactForm from '../react/ContactForm';
---

<!-- 
  This server island ensures Actions endpoints are created.
  The React component will hydrate client-side and use Actions via useActionState.
-->
<ContactForm client:visible />

